{
  "installCommand": "corepack enable && corepack use pnpm@10.33.0 && pnpm --prefix chat install --frozen-lockfile",
  "buildCommand": "pnpm --prefix chat build",
  "outputDirectory": "chat/.next",
  "framework": "nextjs"
}
