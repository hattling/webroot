{
  "installCommand": "corepack prepare pnpm@9.12.3 --activate && pnpm --prefix chat install --frozen-lockfile",
  "buildCommand": "pnpm --prefix chat build",
  "outputDirectory": "chat/.next",
  "framework": "nextjs"
}
