{
  "installCommand": "corepack enable && corepack prepare pnpm@10.33.0 --activate && pnpm --prefix chat install --frozen-lockfile",
  "buildCommand": "pnpm --prefix chat build",
  "outputDirectory": "chat/.next",
  "framework": "nextjs"
}
