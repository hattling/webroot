{
  "installCommand": "node -v && npm -v && which pnpm && pnpm -v && corepack enable && corepack use pnpm@10.33.0 && which pnpm && pnpm -v && corepack pnpm -v && corepack pnpm config get registry && corepack pnpm --prefix chat install",
  "buildCommand": "corepack pnpm --prefix chat build",
  "outputDirectory": "chat/.next"
}
